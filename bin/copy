#!/bin/bash
case "$(uname -s | tr '[:upper:]' '[:lower:]')" in
    linux) 
        if command -v xclip &>/dev/null;
            then cat - | xclip -sel clip
        else
            echo 'xclip not installed'>&2;
            exit 127
        fi
        ;;
    darwin) cat - | pbcopy;
esac