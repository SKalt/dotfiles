#!/bin/sh
set -eu
name="${1:-}"
if [ -z "$name" ]; then
  printf "branch name: "
  read -r name
fi
trunk=""; trunk="$(~/bin/git-main-branch || echo "main")"

git fetch origin "$trunk"
git checkout -b "$name" "origin/$trunk"
