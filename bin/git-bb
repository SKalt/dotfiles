#!/bin/sh
set -eu
name="${1:-}"
if [ -z "$name" ]; then
  printf "branch name: "
  read -r name
fi
trunk=""; trunk="$(git var GIT_DEFAULT_BRANCH || :)"
if [ -z "$trunk" ]; then trunk=main; fi

git fetch origin "$trunk"
git checkout -b "$name" "origin/$trunk"
